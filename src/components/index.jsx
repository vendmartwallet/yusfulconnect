
import tokenAbi from "../constants/abi.json";
import tokenContract from "../constants/contractAddress.json";

const contractDetails = {
	saver: {
		tokenAbi: tokenAbi,
		tokenContract: tokenContract,
	},
};

// module.exports = { contractDetails };
export { contractDetails };
